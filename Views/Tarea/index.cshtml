@{
    ViewData["Title"] = "Home Page";
}
@model List<Usuario>
    <h2>Product List</h2>

    <table class="table table-dark">
        <thead>
            <tr>
                <th hidden>ID</th>
                <th>Nombre</th>
                @* <th></th> *@
            </tr>
        </thead>
        <tbody>
            @foreach (var usuario in Model)
            {
                <tr>
                    <td hidden>@usuario.Id</td>
                    <td>@usuario.NombreDeUsuario</td>
                    <td>
                    <a asp-controller="Usuario" asp-action="ModificarUsuario" asp-route-idProducto="@usuario.Id">Modificar</a>
                    <a asp-controller="Usuario" asp-action="EliminarUsuario" asp-route-idProducto="@usuario.Id" onclick="return confirm('¿Estas seguro que quieres eliminar el elemento?')">Eliminar</a>
                    </td> 
                </tr>
 
            }
        </tbody>
    </table>
